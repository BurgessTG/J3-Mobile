# iOS Host App

This folder holds the SwiftUI host shell for the shared KMP app.

The host app should import the `ComposeApp` framework produced by the shared module and embed `MainViewController()` from `composeApp`.
